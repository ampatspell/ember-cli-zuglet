# Functions

``` javascript
let hello = store.functions.callable('hello');
let result = await hello.call({ name: 'zeeba' });
```

``` json
{
  "data": {
    "message": "hello, zeeba!"
  }
}
```

> TODO
