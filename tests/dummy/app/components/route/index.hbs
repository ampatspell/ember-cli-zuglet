<div class="route-index">

  <div class="row">
    <Json @value={{this.doc}}/>
  </div>

  {{!-- <div class="row">
    <input type="button" value="Toggle" {{on "click" this.toggle}}/>
  </div>

  <div class="row">
    <Json @value={{this.zeeba}}/>
    <Json @value={{this.larry}}/>
  </div>

  <div class="row">
    {{#each this.models as |model|}}
      <div>{{model}} {{model.type}} {{model.name}}</div>
    {{/each}}
  </div>

  --}}

  {{!-- <div class="row">
    {{#each this.query.content as |doc|}}
      <div>{{doc.data.type}} {{doc.type}}</div>
    {{/each}}
  </div> --}}

</div>